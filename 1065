#include <stdio.h>
 
int main() {
    int num, cont_par;
    
    for(int i=1; i<=5; i++){
        scanf("%d", &num);
        if(num % 2 == 0)
            cont_par++;
    }
    printf("%d valores pares\n", cont_par);
    return 0;
}
